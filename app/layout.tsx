// app/layout.tsx

import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "CSV Runner Dashboard | Running Data Analytics",
  description: "Upload and analyze running data from CSV files with interactive visualizations and comprehensive statistics.",
  keywords: ["running", "CSV", "dashboard", "analytics", "visualization"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  );
}